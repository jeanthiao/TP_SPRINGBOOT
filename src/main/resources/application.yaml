spring:
  application:
    name: tp1

  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/gestion_commande

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

    jwt:
      secret: "ma-cle-secrete-super-longue-pour-jwt-256bits!!"
      expiration: 86400000  # 24 heures en ms
